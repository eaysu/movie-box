# Testler backend/tests altında; paketler (app, scripts) backend/ kökünden
# içe aktarılıyor. `pythonpath` olmadan depo kökünden `pytest` çalışmaz.
[pytest]
pythonpath = backend
testpaths = backend/tests
